package com.capg.Learn;

import java.io.IOException;

public class Atm extends Optional {
    public static void main(String[] args) throws IOException {
        Optional optional = new Optional();

        optional.getLogin();
    }
}